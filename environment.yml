name: rag_chatbot
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - numpy
  - pandas
  - matplotlib      # ✨ เพิ่มใหม่
  - seaborn         # ✨ เพิ่มใหม่
  - jupyter
  - pip:
      - sentence-transformers
      - faiss-cpu
      - openai
      - transformers
      - torch
      - tqdm