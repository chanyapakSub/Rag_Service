# 🤖 RAG Chatbot with Guardrails

Chatbot ที่ตอบคำถามจาก documents โดยใช้ RAG (Retrieval-Augmented Generation) พร้อมระบบตรวจสอบความปลอดภัยของคำตอบ

## ขั้นตอนทำงาน

ระบบนี้ทำงาน 3 ขั้นตอน:
1. **Search**: หาข้อมูลที่เกี่ยวข้องจาก documents (933 chunks)
2. **Generate**: ใช้ Gemini API สร้างคำตอบจากข้อมูลที่หาได้
3. **Filter**: ตรวจสอบว่าคำตอบปลอดภัยด้วย Bielik-Guard + keyword filtering

---

## 📁 โครงสร้างโปรเจกต์

```
RAG_SERVICE/
│
├──  APPLICATIONS (เลือกรันตัวใดตัวหนึ่ง)
│   ├── app_guardrail_bielik.py          ⭐ ใช้ตัวนี้ (แนะนำ!)
│   │                                       - ใช้ Bielik-Guard AI model
│   │                                       - ใช้ BGE-base embeddings (แม่นกว่า)
│   │                                       - UI ครบ, settings ปรับได้เยอะ
│   │
│   └── app_guardrail_model.py           ❌ embeding + classifier
│                                           - ส่งโมเดลให้ดูไม่ได้ แต่ถ้าได้เรียกสัมเอาไปโชว์ได้ค่า
│                                            
│
├── CONFIGURATION
│   ├── .env                              ต้องสร้างเอง! (ใส่ GEMINI_API_KEY)
│   ├── requirements.txt                  ใช้ติดตั้ง dependencies
│   └── README.md                        📖 ไฟล์นี้
│
├── DATA
│   └── data_rag/
│       ├── documents.csv                         เอกสารต้นฉบับ (20 docs)
│       ├── single_passage_answer_questions.csv   ทดสอบ (40 คำถาม)
│       ├── multi_passage_answer_questions.csv    ทดสอบ (40 คำถาม)
│       └── no_answer_questions.csv               ทดสอบ (40 คำถาม)
│
├── NOTEBOOKS (Pipeline สร้าง RAG System)
│   └── notebooks/
│       ├── 01_eda.ipynb                 ℹ️ Optional - วิเคราะห์ข้อมูล
│       │                                   → สร้าง eda_summary.json
│       │
│       ├── 02_chunking_embedding.ipynb  ⭐ สำคัญ! - สร้าง embeddings
│       │                                   Input: documents.csv
│       │                                   Output: chunks_bge/
│       │                                   เวลา: ~3-5 นาที
│       │
│       ├── 03_vector_database.ipynb     ⭐ สำคัญ! - สร้าง ChromaDB
│       │                                   Input: chunks_bge/
│       │                                   Output: chroma_db_bge/
│       │                                   เวลา: ~1-2 นาที
│       │
│       └── eda_summary.json             ℹ️ Output จาก notebook 1
│
├── GUARDRAIL (Optional - สำหรับ train custom model)
│   └── guardrail_script/
│       ├── train_binary.ipynb           ❌ script สร้าง guardrail แบบ embeding +classifier
│       └── test_benchmark.ipynb         ❌ script ทดสอบความสามารถของ guardrail มีของ 
│                                            มีของ Bielik-Guard ด้วย เข้าไปดูได้นะคะ
│
├── MODELS (Generated by Notebooks)
│   └── models/
│       ├── chroma_db/                   ❌ เวอร์ชันเก่า (MiniLM)
│       │   ├── chroma.sqlite3              - อันนี้ตอนทดลองทดแล้วไม่เวิร์คนะคะ 
│       │   └── [UUID]/                     - 
│       │
│       ├── chroma_db_bge/               ⭐ ใช้ตัวนี้! (BGE embeddings)
│       │   ├── chroma.sqlite3              - ฐานข้อมูลหลัก
│       │   └── f77d9e5f-.../               - Vector index
│       │       ├── data_level0.bin
│       │       ├── header.bin
│       │       ├── length.bin
│       │       └── link_lists.bin
│       │
│       ├── chunks/                      ❌ เวอร์ชันเก่า (MiniLM)
│       │   ├── embeddings.npy              - 467 chunks
│       │   ├── chunk_metadata.csv
│       │   ├── chunks_data.json
│       │   └── chunking_summary.json
│       │
│       ├── chunks_bge/                  ⭐ ใช้ตัวนี้! (BGE embeddings)
│       │   ├── embeddings.npy              - 933 chunks × 768 dim
│       │   ├── chunk_metadata.csv          - ข้อมูล chunks
│       │   ├── chunks_data.json            - Full data
│       │   └── chunking_summary.json       - Config ที่ใช้
│       │
│       └── vector_db_summary.json       ℹ️ สถิติ database


```

---

## 📋 สรุปไฟล์ที่ใช้ vs ไม่ใช้

###  ไฟล์ที่ใช้งานจริง

| ไฟล์/โฟลเดอร์ | ประเภท | ใช้ทำอะไร |
|--------------|--------|-----------|
| **app_guardrail_bielik.py** | App | 🚀 รันตัวนี้! |
| **.env** | Config | API keys |
| **requirements.txt** | Config | Dependencies |
| **data_rag/** | Data | เอกสาร + คำถามทดสอบ |
| **notebooks/02_chunking_embedding.ipynb** | Pipeline | สร้าง embeddings |
| **notebooks/03_vector_database.ipynb** | Pipeline | สร้าง ChromaDB |
| **models/chroma_db_bge/** | Database | Vector DB ที่ใช้ |
| **models/chunks_bge/** | Data | Chunks + embeddings |

### ไฟล์ที่ไม่ใช้แล้ว

| ไฟล์/โฟลเดอร์ | เหตุผล |
|--------------|--------|
| **app_guardrail_model.py** | ไม่นึก gaudrail ภายนอกแต่ใช้ที่ train เอง |
| **models/chroma_db/** | เก่า, accuracy ต่ำกว่า |
| **models/chunks/** | เก่า, chunk ขนาดใหญ่เกิน |
| **guardrail_script/** | เป็น code ที่ทำในงานทีสิสของหนู แต่ไม่สามารถเผยแพร่ตัว model กับ data ได้ค่ะ แต่วิธีการและ paper สามารถอ่านได้นะคะ |


## วิธีใช้งาน

### 1. ติดตั้ง Dependencies

```bash
# Clone repo
git clone <repo-url>
cd RAG_SERVICE

# สร้าง virtual environment
python -m venv venv
source venv/bin/activate  # Windows: venv\Scripts\activate

# ติดตั้ง packages
pip install -r requirements.txt

# ติดตั้งเพิ่มสำหรับ Streamlit
pip install streamlit chromadb google-generativeai python-dotenv transformers
```

### 2. Setup API Key

สร้างไฟล์ `.env` ที่ root:
```env
GEMINI_API_KEY=your_gemini_api_key_here
```

ขอ API key: https://makersuite.google.com/app/apikey

### 3. รัน Application

```bash
streamlit run app_guardrail_bielik.py
```

เปิดเบราว์เซอร์ที่: http://localhost:8501

---

## Pipeline (ถ้าต้องการสร้างใหม่)

### กรณี models/ มีไฟล์ครบแล้ว → ข้าม Step นี้

ถ้า `models/chroma_db_bge/` และ `models/chunks_bge/` มีไฟล์อยู่แล้ว:
```bash
# รันได้เลย!
streamlit run app_guardrail_bielik.py
```

### กรณีต้องการสร้าง Database ใหม่

**Step 1: สร้าง Chunks + Embeddings** (~3-5 นาที)
```bash
jupyter notebook notebooks/02_chunking_embedding.ipynb

# รัน cells ทั้งหมด
# Output: models/chunks_bge/
#   - embeddings.npy (933 chunks × 768 dim)
#   - chunk_metadata.csv
#   - chunks_data.json
```

**Step 2: สร้าง ChromaDB** (~1-2 นาที)
```bash
jupyter notebook notebooks/03_vector_database.ipynb

# รัน cells ทั้งหมด
# Output: models/chroma_db_bge/
```

**Step 3: รัน App**
```bash
streamlit run app_guardrail_bielik.py
```

---

## Features

### RAG System
-  **933 chunks** (เพิ่มจาก 467 - ครอบคลุมมากขึ้น)
-  **BGE-base-en-v1.5** embeddings (768 มิติ - แม่นกว่า MiniLM 5-8%)
-  **Chunk size 156 คำ** (เดิม 300 - เฉพาะเจาะจงกว่า)
-  Semantic search ด้วย ChromaDB
-  ปรับ similarity threshold ได้

### Safety Guardrails
-  **Bielik-Guard 0.1B** - AI model ตรวจสอบเนื้อหา
-  Topic blocking (ศาสนา, การเมือง, ความรุนแรง)
-  Keyword filtering
-  ปรับ sensitivity ได้ (0.3-0.9)

### UI (Streamlit)
-  Web interface สะดวก
-  แสดง similarity scores
-  แสดงข้อมูลที่ retrieve มา
-  Safety controls แบบ real-time

---

##  Performance

### เปรียบเทียบ Old vs New

| Metric | เดิม (MiniLM + 300 words) | ใหม่ (BGE + 156 words) | ดีขึ้น |
|--------|---------------------------|------------------------|--------|
| **จำนวน Chunks** | 467 | **933** | +100% |
| **Avg Similarity** | 0.47 | **0.62** | +32%  |
| **Top-1 Accuracy** | 68% | **85%** | +17%  |
| **Query Time** | 15ms | 18ms | -20% |

### ตัวอย่างคำถาม

```python
คำถาม: "What drops a key upon death?"

เดิม:
├─ Model: all-MiniLM-L6-v2
├─ Chunk: 300 words
└─ Similarity: 0.47 ❌ (ต่ำเกิน, อาจไม่ผ่าน threshold)

ใหม่:
├─ Model: BGE-base-en-v1.5
├─ Chunk: 156 words
└─ Similarity: 0.65  (สูงขึ้น 38%!)
```

---

##  Configuration

### Chunking Settings

ดูใน `models/chunks_bge/chunking_summary.json`:
```json
{
  "num_documents": 20,
  "num_chunks": 933,
  "chunk_size": 156,        // คำต่อ chunk (เล็กลง = เฉพาะเจาะจง)
  "overlap": 31,            // คำที่ overlap (20% ของ chunk_size)
  "embedding_model": "BAAI/bge-base-en-v1.5",
  "embedding_dim": 768
}
```

**ทำไมใช้ settings นี้?**
-  Chunk 156 คำ → เล็กกว่าเดิม (300) → similarity สูงขึ้น
-  BGE-base → แม่นกว่า MiniLM 5-8%
-  933 chunks → ครอบคลุมดีกว่า 467 chunks

### Retrieval Settings

ใน `app_guardrail_bielik.py` บรรทัด 71:
```python
def retrieve_documents(
    query: str, 
    min_similarity: float = 0.5,  #  ปรับตรงนี้
    top_k: int = 8                #  จำนวน chunks
):
```
---

## Models ที่ใช้

### 1. Embedding: BGE-Base-EN-v1.5 

```python
from sentence_transformers import SentenceTransformer
model = SentenceTransformer('BAAI/bge-base-en-v1.5')
```

**Specs:**
- Dimensions: 768
- Accuracy: 84-87% (MTEB benchmark)
- Developer: Beijing Academy of AI
- License: MIT

**ทำไมเลือก BGE?**
-  แม่นกว่า MiniLM 5-8%
-  Rank #1 ขนาดเดียวกันบน MTEB
-  v1.5 แก้ไข similarity distribution

### 2. Safety: Bielik-Guard-0.1B 

```python
from transformers import pipeline
guard = pipeline("text-classification", model="speakleash/Bielik-Guard-0.1B-v1.0")
```

**Specs:**
- Size: 100M parameters
- Categories: Safe / Harmful / Neutral
- Purpose: Content moderation

### 3. LLM: Google Gemini 

```python
import google.generativeai as genai
genai.configure(api_key=os.getenv("GEMINI_API_KEY"))
model = genai.GenerativeModel('gemini-2.5-flash')
```

**Model:** gemini-pro (text generation)

---

##  Quick Commands

```bash
# รัน app
streamlit run app_guardrail_bielik.py

# Rebuild database (ถ้าจำเป็น)
jupyter notebook notebooks/02_chunking_embedding.ipynb  # 3-5 นาที
jupyter notebook notebooks/03_vector_database.ipynb     # 1-2 นาที

# ตรวจสอบ configuration
cat models/chunks_bge/chunking_summary.json
```
